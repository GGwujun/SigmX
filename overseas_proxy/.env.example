# Shared secret between this proxy and the CN main service.
# Generate a random one: python -c "import secrets; print(secrets.token_urlsafe(24))"
# Must match PROXY_SECRET in the CN main service's agent/.env.
PROXY_SECRET=change-me-to-a-random-secret

# Listen port (default 12000)
PORT=12000
